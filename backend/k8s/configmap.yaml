apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  DB_USER: postgres
  DB_PASSWORD: password
  DB_HOST: postgres-service
  DB_PORT: "5432"
  DB_NAME: MicroServices
  NODE_ENV: development
  REGION: us-east-2
